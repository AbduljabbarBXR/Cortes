# Cortes web app env defaults (all optional; keys can be set in Settings instead)
# Anything VITE_* is baked into the JS bundle. Do NOT put a real key here.

VITE_DEEPSEEK_API_KEY=

VITE_OPENROUTER_API_KEY=
VITE_OPENROUTER_MODEL=openai/gpt-oss-20b:free
